/* tslint:disable:no-unused-variable */

import { TestBed, waitForAsync } from '@angular/core/testing';
import { PadPipe } from './pad.pipe';

describe('Pipe: Pade', () => {
  it('create an instance', () => {
    let pipe = new PadPipe();
    expect(pipe).toBeTruthy();
  });
});
